{
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "start": "http-server",
    "build": "tsc",
    "copy-static": "copy-static.sh",
    "deploy": "git subtree push --prefix=dist origin gh-pages"
  }
}
